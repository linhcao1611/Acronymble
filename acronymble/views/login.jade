extends layout

block content
	h1= title
	if message
		p. 
			#{message}
	form(name="login", action="/users/login", method="post")
		input(type="text", name="username")
		input(type="password", name="password")
		input(type="submit", value="login")
	a(href="/users/signup", class="btn").
		Signup